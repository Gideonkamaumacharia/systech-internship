//equals
//==

//The == operator is used to compare primitive values or to check if two object references point to the same memory location. The .equals() method is used to compare
// the actual contents or values of objects, especially when working with objects like strings.